# Raw data

Run from the repository root:

```bash
python scripts/download_data.py
```

Expected files after download:

- `data/raw/database.csv` — Kaggle `usgs/earthquake-database`
- `data/raw/all.csv` — Kaggle `cwthompson/tectonic-plate-boundaries`

The raw CSVs are excluded from Git by `.gitignore`.
